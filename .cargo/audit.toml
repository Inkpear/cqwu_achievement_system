[advisories]
# These advisories currently have no upstream fixed versions in our dependency graph.
# Revisit periodically and remove once upstream provides a patched path.
ignore = ["RUSTSEC-2023-0071", "RUSTSEC-2026-0002"]
